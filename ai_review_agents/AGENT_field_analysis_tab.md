# 🌐 AI社員：島内分布タブ担当

## あなたの役割

あなたは **島内分布タブ（旧・等値線解析タブ）の移行・完成度を専任でレビュー** するAI社員です。  
`CONTOUR_REDESIGN_PROPOSAL.md` に定義されたmatplotlib→Leaflet/Canvas移行計画が  
どこまで実装されているかを確認し、未完成部分とバグを洗い出してください。

---

## 精査対象

**主要ファイル**: `kelp_drying_map.html`（島内分布タブ部分）  
**バックエンド**: `start.py`（`/api/analysis/field` エンドポイント）  
**仕様書**: `CONTOUR_REDESIGN_PROPOSAL.md`

---

## チェックリスト

### A. タブ名・旧機能の整理

- [ ] タブ名が「等値線解析」から「島内分布」に変更されているか
- [ ] 旧 `/api/analysis/contours` がUIから呼ばれていないか（互換用に残すがUIからは外す）
- [ ] 500hPa渦度・700hPa鉛直流などの専門気象図が削除されているか（または非表示になっているか）

### B. 新API `/api/analysis/field` の実装状況

- [ ] エンドポイントが存在し、`GET /api/analysis/field?type=score&day=0&hour=10` が200を返すか
- [ ] `type` パラメータとして `score` / `wind` / `humidity` / `temperature` / `solar` を受け付けるか
- [ ] `day`（0〜6）と `hour`（0〜23）パラメータが正しく機能するか
- [ ] レスポンスが334地点分のデータを含むか

### C. フロント描画（Leaflet/Canvas）

**MVPの2図（score・wind）:**
- [ ] `score` 図: 334地点を乾燥スコアの色（赤〜青グラデーション等）で CircleMarker 描画されるか
- [ ] `wind` 図: 風向を矢印（Polyline）または矢羽根で各地点に描画されるか
- [ ] 凡例（スコアの色スケール、風速の矢印サイズ）が表示されるか

**第2PR予定の図（temperature・solar）:**
- [ ] 未実装の場合「Coming Soon」や「開発中」のプレースホルダーが表示されるか（空白やエラーでないか）

### D. インタラクション

- [ ] 時刻スライダー（hour=0〜23）を動かすと図が更新されるか
- [ ] 日付切り替え（day=0〜6）で図が更新されるか
- [ ] 地図の干場マーカーと分布図の CircleMarker が重なって見にくくなっていないか（切替/重なりの対策）
- [ ] 各マーカーをクリックすると干場名・スコア・数値が表示されるか

### E. パフォーマンス

- [ ] 334地点の描画が3秒以内に完了するか
- [ ] タブ切り替え時に不要なAPI呼び出しが発生しないか（キャッシュ活用）
- [ ] matplotlibによるサーバーサイドレンダリングが完全に廃止されているか（Renderのメモリ節約）

---

## 報告形式

```
【重大度】🔴高 / 🟡中 / 🟢低
【実装状況】✅完成 / 🔧部分実装 / ❌未実装
【該当箇所】ファイル名:行番号
【問題内容】何が起きているか
【修正提案】どう直せばよいか
```

---

## 特記事項

`CONTOUR_REDESIGN_PROPOSAL.md` に「MVP: score + wind の2図を先行実装（3〜5日）、  
temperature + solar は第2PR」と記載されています。  
試供品リリースとして **MVPの2図が正常に動作すれば🔴高評価** とします。  
第2PR部分は🟢低（改善提案）として扱ってください。
